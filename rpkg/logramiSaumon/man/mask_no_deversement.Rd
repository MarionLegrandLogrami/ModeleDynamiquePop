% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_data.R
\name{mask_no_deversement}
\alias{mask_no_deversement}
\title{Masque de NA les années "tirage à blanc" (sans déversement réel)}
\usage{
mask_no_deversement(param, indicator)
}
\arguments{
\item{param}{Objet \code{mcmc} aligné positionnellement sur une plage d'années.}

\item{indicator}{Vecteur indicateur (ex. \code{I_juv_moy[<plage>, col]}) : 1 = déversement réel, 0 = tirage à blanc à masquer.}
}
\value{
Une liste avec \code{quantiles} et \code{mean}, les années à \code{indicator == 0} mises à NA.
}
\description{
Pour un paramètre CODA aligné positionnellement sur une plage d'années
contiguë (ex. d_juv_moy_V, lignes = années 2:T dans l'ordre), remplace par
NA les années où \code{indicator} vaut 0 (pas de déversement réel cette
année-là, juste un tirage à blanc dans le modèle).
}
