% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_graph.R
\name{draw_indic_tx_ren}
\alias{draw_indic_tx_ren}
\title{Graphique ggplot de l'indicateur taux de renouvellement (ligne, moyenne mobile 5 ans)}
\usage{
draw_indic_tx_ren(
  tx_renouv,
  pngName,
  Tyear = T,
  typ_pop = "sauvage",
  simulation = FALSE,
  png = FALSE,
  scenario = NULL,
  pngWidth = 1000,
  pngHeight = 800,
  annee_debut = 1985,
  annee_fin = (1974 + Tyear - 6),
  rect_yStart = c(0, 1),
  rect_yEnd = c(1, 5),
  rect_col = c("red", "green")
)
}
\arguments{
\item{tx_renouv}{Matrice du taux de renouvellement en échelle log (itérations x années).}

\item{pngName}{Nom du fichier PNG à enregistrer (sans extension), si \code{png = TRUE}.}

\item{Tyear}{Nombre d'années de la période rétrospective. Défaut : la variable globale \code{T}.}

\item{typ_pop}{Type de population ("sauvage" ou "totale"), utilisé dans le titre du graphique. Défaut : "sauvage".}

\item{simulation}{Si TRUE, ajoute 20 ans de simulation à \code{Tyear}, une ligne verticale et les annotations "Rétrospectif"/"Simulation". Défaut : FALSE.}

\item{png}{Si TRUE, enregistre le graphique en PNG dans \code{imgwd} (demandé interactivement si absent). Défaut : FALSE.}

\item{scenario}{Sous-titre du graphique (nom du scénario). Défaut : aucun.}

\item{pngWidth, pngHeight}{Dimensions du PNG en pixels si \code{png = TRUE}. Défaut : 1000 x 800.}

\item{annee_debut, annee_fin}{Bornes d'années affichées sur le graphique. Défaut : 1985 à \code{1974 + Tyear - 6}.}

\item{rect_yStart, rect_yEnd, rect_col}{Bornes basses/hautes et couleurs des rectangles de fond ("bon"/"mauvais"). Défaut : rouge sous 1, vert au-dessus.}
}
\value{
Une liste de 2 éléments : le graphique ggplot, et \code{mean_data} (moyenne par année, échelle naturelle, avant lissage).
}
\description{
Trace le taux de renouvellement (échelle log naturelle) en moyenne mobile
5 ans, avec un fond rouge/vert selon que le taux est en dessous/au-dessus
de 1. Peut optionnellement enregistrer un PNG et annoter une période de
simulation (vs rétrospective).
}
