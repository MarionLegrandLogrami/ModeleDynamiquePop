% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_graph.R
\name{custom_plot}
\alias{custom_plot}
\title{Boxplot par année (base R), pour les sorties du modèle}
\usage{
custom_plot(
  data,
  years,
  yr_deb = NULL,
  yr_point = NULL,
  main_title = NULL,
  ylab_txt = NULL,
  ylim_range = c(0, 1),
  subplot_label = "",
  col_box = "coral3",
  color_dev = NULL,
  sector_color = 1,
  col_active = NULL,
  col_inactive = NULL,
  add_segments = NULL,
  add_text = NULL,
  year_origin = 1974,
  y_at = NULL,
  center_line = NULL,
  points_overlay = NULL,
  mean_line = NULL,
  mean_line_range = NULL,
  mean_label_digits = 5,
  mean_label_line = 0,
  lang = "fr"
)
}
\arguments{
\item{data}{Matrice/data.frame des quantiles par année (lignes = années, colonnes 1:5 = quantiles 5/25/50/75/95\%).}

\item{years}{Vecteur des indices d'années (positions dans \code{data}) à tracer.}

\item{yr_deb}{Indice (dans \code{years}) à partir duquel commencer l'affichage. Défaut : 1 (depuis le début).}

\item{yr_point}{Indices (dans \code{years}) à afficher comme point unique plutôt que boxplot.}

\item{main_title}{Titre du graphique. Défaut : aucun titre.}

\item{ylab_txt}{Titre de l'axe Y. Défaut : texte localisé selon \code{lang}.}

\item{ylim_range}{Bornes de l'axe Y. Défaut : \code{c(0, 1)}.}

\item{subplot_label}{Étiquette affichée en haut à droite du graphique (ex. lettre de sous-figure).}

\item{col_box}{Couleur de boîte par défaut. Défaut : "coral3".}

\item{color_dev}{Vecteur indicateur (0/1/NA) par année, pour colorer selon le déversement (cf. \code{sector_color}, \code{col_active}/\code{col_inactive}).}

\item{sector_color}{Si \code{col_active}/\code{col_inactive} non fournis : 1 = palette corail/bisque, autre = palette vert foncé/clair.}

\item{col_active, col_inactive}{Couleurs personnalisées pour \code{color_dev == 1} / \code{== 0}.}

\item{add_segments}{Liste de segments additionnels à tracer (chaque élément : \code{x0, y0, x1, y1, col, lty, lwd}).}

\item{add_text}{Liste de textes additionnels à afficher (chaque élément : \code{x, y, labels, col, cex, pos, offset}).}

\item{year_origin}{Année de référence (année 1 du modèle = year_origin + 1). Défaut : 1974.}

\item{y_at}{Positions personnalisées des graduations de l'axe Y. Défaut : graduations automatiques.}

\item{center_line}{Vecteur (aligné sur \code{years}) de moyennes arithmétiques à tracer comme trait central, à la place de la médiane (colonne 3 de \code{data}) par défaut.}

\item{points_overlay}{Points observés à superposer (liste avec \code{x, y, pch, col, cex}), ex. comptages exhaustifs quand une station fonctionne.}

\item{mean_line}{Valeur y d'une ligne rouge horizontale de moyenne/médiane générale, avec étiquette dans la marge de droite.}

\item{mean_line_range}{Étendue x de \code{mean_line}. Défaut : \code{c(min(years), max(years))}.}

\item{mean_label_digits}{Nombre de décimales de l'étiquette de \code{mean_line}. Défaut : 5.}

\item{mean_label_line}{Position (paramètre \code{line} de \code{mtext}) de l'étiquette de \code{mean_line}. Défaut : 0.}

\item{lang}{Langue des titres par défaut des axes : "fr" ou "en". Défaut : "fr".}
}
\value{
Rien (effet de bord : dessine sur le device graphique actif).
}
\description{
Trace un boxplot personnalisé par année (quantiles 5/25/50/75/95\%
attendus en colonnes 1:5 de \code{data}), avec gestion des couleurs par
secteur/déversement, points isolés, segments et texte additionnels, ligne
de moyenne/médiane générale, etc. Fonction "base R" (pas ggplot),
utilisée directement ou via \code{\link{boxplot_years_png}}.
}
